<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - <%= name %></title>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            padding: 20px;
            background-color: #f0f2f5;
            margin: 0;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .profile-info {
            margin-bottom: 20px;
        }

        .profile-info label {
            font-weight: bold;
        }

        .profile-info input {
            width: 100%;
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .buttons {
            text-align: center;
            margin-top: 20px;
        }

        .buttons button {
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            background-color: #007BFF;
            color: white;
            cursor: pointer;
            margin: 5px;
        }

        .buttons button:hover {
            background-color: #0056b3;
        }

        .message {
            text-align: center;
            color: green;
            margin-bottom: 20px;
        }

        .top-left-button {
            position: absolute;
            top: 20px;
            left: 20px;
        }
    </style>
    <script>
        function enableEditing() {
            document.getElementById('name').disabled = false;
            document.getElementById('saveButton').style.display = 'inline-block';
            document.getElementById('editButton').style.display = 'none';
        }
    </script>
</head>

<body>
    <div class="top-left-button buttons">
        <form action="/" method="get" style="display: inline;">
            <button type="submit">Home</button>
        </form>
    </div>
    <div class="container">
        <h1>Welcome <%= name %></h1>
        <% if (typeof message !== 'undefined') { %>
        <div class="message">
            <%= message %>
        </div>
        <% } %>
        <form action="/user/profile" method="post">
            <div class="profile-info">
                <label for="name">Name:</label>
                <input type="text" id="name" name="newName" value="<%= name %>" disabled>
                <label for="email">Email:</label>
                <input type="email" id="email" name="newEmail" value="<%= email %>" disabled>
            </div>
            <div class="buttons">
                <button type="button" id="editButton" onclick="enableEditing()">Edit</button>
                <button type="submit" id="saveButton" style="display: none;">Save</button>
            </div>
        </form>
        <div class="buttons">
            <form action="/user/logout" method="get" style="display: inline;">
                <button type="submit">Logout</button>
            </form>
        </div>
    </div>
</body>

</html>